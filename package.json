{
  "name": "rabbit-editor",
  "version": "1.0.3",
  "description": "A bare bone Text Editor library for javascript, Aimed to be small, simple, efficient and easy to extend.",
  "main": "dist/index.mjs",
  "files": [
    "./dist"
  ],
  "scripts": {
    "dev": "vite",
    "build:page": "vite build && echo 'rabbit.codedynasty.dev' >> dist/CNAME",
    "build": "deno run --allow-all build.ts && tsup lib/index.ts && npm run pack",
    "pack": "npm pack && mv Rabbit.js-0.0.0.tgz ~",
    "deploy": "npm run build:page && git add -f dist/ && git commit -m build-dist && (git push origin --delete gh-pages || true) && git subtree push --prefix dist origin gh-pages && git reset HEAD~1"
  },
  "keywords": [
    "text editor",
    "rabbit editor"
  ],
  "author": "friday candour",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2",
    "vite": "^4.4.9"
  }
}
